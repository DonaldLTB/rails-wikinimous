<div class="container">
  <h2>Wikinimous</h2>
  <%= link_to '➕New Article', new_article_path %>
<% @articles.each do |article| %>
  <h5><%= link_to article.title, article_path(article.id) %></h5>
  <p><%= article.content %></p>
<% end %>
</div>
